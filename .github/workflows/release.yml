name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-upload:
    name: Build for ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: spider
            asset_name: spider-linux-amd64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: spider.exe
            asset_name: spider-windows-amd64.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: spider
            asset_name: spider-macos-amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: spider
            asset_name: spider-macos-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Browser Stealth Script
        run: curl -L -o stealth.min.js https://raw.githubusercontent.com/requireCool/stealth.min.js/main/stealth.min.js

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev pkg-config

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary (Unix)
        if: runner.os != 'Windows'
        run: mv target/${{ matrix.target }}/release/${{ matrix.artifact_name }} ${{ matrix.asset_name }}-${{ github.ref_name }}

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        run: mv target/${{ matrix.target }}/release/${{ matrix.artifact_name }} ${{ matrix.asset_name }}-${{ github.ref_name }}.exe

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ matrix.asset_name }}-*